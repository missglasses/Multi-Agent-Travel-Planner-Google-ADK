import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { appendInstructions } from "../../models/llm_request.js";
import { requireAgent } from "../invocation_context.js";
import { isLlmAgent } from "../llm_agent.js";
import { BaseLlmRequestProcessor } from "./base_llm_processor.js";
const _IdentityLlmRequestProcessor = class _IdentityLlmRequestProcessor extends BaseLlmRequestProcessor {
  /**
   * Appends agent name and description as identity instructions to the system
   * prompt of the request, unless the agent has no reachable transfer target.
   *
   * @param invocationContext - The current invocation context.
   * @param llmRequest - The request object to append instructions to.
   */
  // eslint-disable-next-line require-yield
  async *runAsync(invocationContext, llmRequest) {
    const agent = requireAgent(invocationContext);
    if (isLlmAgent(agent) && agent.disallowTransferToParent && agent.disallowTransferToPeers && agent.subAgents.length === 0) {
      return;
    }
    const si = [`You are an agent. Your internal name is "${agent.name}".`];
    if (agent.description) {
      si.push(`The description about you is "${agent.description}"`);
    }
    appendInstructions(llmRequest, si);
  }
};
__name(_IdentityLlmRequestProcessor, "IdentityLlmRequestProcessor");
let IdentityLlmRequestProcessor = _IdentityLlmRequestProcessor;
const IDENTITY_LLM_REQUEST_PROCESSOR = new IdentityLlmRequestProcessor();
export {
  IDENTITY_LLM_REQUEST_PROCESSOR,
  IdentityLlmRequestProcessor
};
