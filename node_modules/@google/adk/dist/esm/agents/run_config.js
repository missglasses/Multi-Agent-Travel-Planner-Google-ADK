import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { logger } from "../utils/logger.js";
var StreamingMode = /* @__PURE__ */ ((StreamingMode2) => {
  StreamingMode2["NONE"] = "none";
  StreamingMode2["SSE"] = "sse";
  StreamingMode2["BIDI"] = "bidi";
  return StreamingMode2;
})(StreamingMode || {});
function createRunConfig(params = {}) {
  var _a;
  validateStreamingMode(params.streamingMode);
  return {
    saveInputBlobsAsArtifacts: false,
    supportCfc: false,
    enableAffectiveDialog: false,
    streamingMode: "none" /* NONE */,
    pauseOnToolCalls: false,
    ...params,
    maxLlmCalls: validateMaxLlmCalls((_a = params.maxLlmCalls) != null ? _a : 500)
  };
}
__name(createRunConfig, "createRunConfig");
function validateStreamingMode(streamingMode) {
  if (streamingMode === "bidi" /* BIDI */) {
    throw new Error(
      "StreamingMode.BIDI is not supported; use StreamingMode.SSE."
    );
  }
}
__name(validateStreamingMode, "validateStreamingMode");
function validateMaxLlmCalls(value) {
  if (value > Number.MAX_SAFE_INTEGER) {
    throw new Error(
      `maxLlmCalls should be less than ${Number.MAX_SAFE_INTEGER}.`
    );
  }
  if (value <= 0) {
    logger.warn(
      "maxLlmCalls is less than or equal to 0. This will result in no enforcement on total number of llm calls that will be made for a run. This may not be ideal, as this could result in a never ending communication between the model and the agent in certain cases."
    );
  }
  return value;
}
__name(validateMaxLlmCalls, "validateMaxLlmCalls");
export {
  StreamingMode,
  createRunConfig
};
