import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { State } from "../sessions/state.js";
import { requireAgent } from "./invocation_context.js";
const _ReadonlyContext = class _ReadonlyContext {
  constructor(invocationContext) {
    this.invocationContext = invocationContext;
  }
  /**
   * The user content that started this invocation.
   */
  get userContent() {
    return this.invocationContext.userContent;
  }
  /**
   * The current invocation id.
   */
  get invocationId() {
    return this.invocationContext.invocationId;
  }
  /**
   * The user ID of the current session.
   */
  get userId() {
    return this.invocationContext.userId;
  }
  /**
   * The ID of the current session.
   */
  get sessionId() {
    return this.invocationContext.session.id;
  }
  /**
   * The current agent name.
   */
  get agentName() {
    return requireAgent(this.invocationContext).name;
  }
  /**
   * The state of the current session.
   */
  get state() {
    return new State(
      this.invocationContext.session.state,
      {}
    );
  }
};
__name(_ReadonlyContext, "ReadonlyContext");
let ReadonlyContext = _ReadonlyContext;
export {
  ReadonlyContext
};
