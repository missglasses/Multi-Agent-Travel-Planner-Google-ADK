import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
const _DynamicNodeState = class _DynamicNodeState {
  constructor() {
    /** Dynamic node runs keyed by unique node path (e.g. `wf/node_a@1`). */
    this.runs = /* @__PURE__ */ new Map();
    /** Union of unresolved interrupt ids across dynamic child nodes. */
    this.interruptIds = /* @__PURE__ */ new Set();
  }
  /** All in-flight dynamic node tasks. */
  getDynamicTasks() {
    return [...this.runs.values()].map((run) => run.task).filter((task) => task !== void 0);
  }
};
__name(_DynamicNodeState, "DynamicNodeState");
let DynamicNodeState = _DynamicNodeState;
export {
  DynamicNodeState
};
