import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { isBaseTool } from "../tools/base_tool.js";
import { FunctionNode } from "./nodes/function_node.js";
import { ParallelWorker } from "./nodes/parallel_worker.js";
import { ToolNode } from "./nodes/tool_node.js";
const FUNCTION_BUILDER = {
  match: /* @__PURE__ */ __name((value) => typeof value === "function", "match"),
  build: /* @__PURE__ */ __name((value, options) => {
    var _a;
    const handler = value;
    const name = (_a = options.name) != null ? _a : handler.name;
    if (!name) {
      throw new Error(
        "node(): the wrapped function has no name; pass {name} explicitly."
      );
    }
    return new FunctionNode(name, handler, options);
  }, "build")
};
const TOOL_BUILDER = {
  match: /* @__PURE__ */ __name((value) => isBaseTool(value), "match"),
  build: /* @__PURE__ */ __name((value, options) => new ToolNode(value, options), "build")
};
const NODE_BUILDERS = [
  FUNCTION_BUILDER,
  TOOL_BUILDER
];
const PARALLEL_WORKER_FACTORY = /* @__PURE__ */ __name((inner, options) => new ParallelWorker(inner, options), "PARALLEL_WORKER_FACTORY");
export {
  NODE_BUILDERS,
  PARALLEL_WORKER_FACTORY
};
