import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { createEvent } from "../events/event.js";
import { errorName } from "./utils/retry_utils.js";
function isNodeErrorEvent(event) {
  return "isNodeError" in event && event.isNodeError === true;
}
__name(isNodeErrorEvent, "isNodeErrorEvent");
const reportedInvocationIds = /* @__PURE__ */ new WeakMap();
function claimNodeErrorReport(error, invocationId) {
  if (typeof error !== "object" || error === null) {
    return true;
  }
  if (reportedInvocationIds.get(error) === invocationId) {
    return false;
  }
  reportedInvocationIds.set(error, invocationId);
  return true;
}
__name(claimNodeErrorReport, "claimNodeErrorReport");
function createNodeErrorEvent(params) {
  var _a, _b;
  const { error, attemptCount, ...eventParams } = params;
  return {
    ...createEvent(eventParams),
    isNodeError: true,
    errorType: errorName(error),
    errorCode: (_a = eventParams.errorCode) != null ? _a : errorCodeOf(error),
    errorMessage: (_b = eventParams.errorMessage) != null ? _b : errorMessageOf(error),
    attemptCount: attemptCount != null ? attemptCount : 1
  };
}
__name(createNodeErrorEvent, "createNodeErrorEvent");
function errorCodeOf(error) {
  const code = error == null ? void 0 : error.code;
  if (typeof code === "string" || typeof code === "number") {
    return String(code);
  }
  return "UNKNOWN_ERROR";
}
__name(errorCodeOf, "errorCodeOf");
function errorMessageOf(error) {
  return error instanceof Error ? error.message : String(error);
}
__name(errorMessageOf, "errorMessageOf");
export {
  claimNodeErrorReport,
  createNodeErrorEvent,
  isNodeErrorEvent
};
