import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { isBaseNode, START } from "../base_node.js";
import { NODE_BUILDERS, PARALLEL_WORKER_FACTORY } from "../node_builders.js";
import { prepareRetryConfig } from "../retry_config.js";
function isPlainObject(value) {
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    return false;
  }
  const proto = Object.getPrototypeOf(value);
  return proto === Object.prototype || proto === null;
}
__name(isPlainObject, "isPlainObject");
function isNodeLike(value) {
  return value === "START" || isBaseNode(value) || NODE_BUILDERS.some((builder) => builder.match(value));
}
__name(isNodeLike, "isNodeLike");
function buildNode(nodeLike, options = {}) {
  if (options.maxParallelWorkers !== void 0 && !options.parallelWorker) {
    throw new Error(
      "maxParallelWorkers can only be set when parallelWorker is true."
    );
  }
  const built = buildInnerNode(nodeLike, options);
  if (options.parallelWorker) {
    if (nodeLike === "START") {
      throw new Error("ParallelWorker cannot wrap a START node.");
    }
    if (!PARALLEL_WORKER_FACTORY) {
      throw new Error(
        "parallelWorker was requested but no ParallelWorker is available; the parallel worker node module is not part of this build."
      );
    }
    return PARALLEL_WORKER_FACTORY(built, {
      maxParallelWorkers: options.maxParallelWorkers
    });
  }
  return built;
}
__name(buildNode, "buildNode");
function buildInnerNode(nodeLike, options) {
  if (nodeLike === "START") {
    return START;
  }
  if (isBaseNode(nodeLike)) {
    return cloneWithOverrides(nodeLike, options);
  }
  for (const builder of NODE_BUILDERS) {
    if (builder.match(nodeLike)) {
      return builder.build(nodeLike, options);
    }
  }
  throw new Error(
    `build_node: unsupported node-like value of type ${typeof nodeLike}. Import the node module that handles it (e.g. via the workflow barrel).`
  );
}
__name(buildInnerNode, "buildInnerNode");
const OVERRIDABLE_KEYS = [
  "name",
  "description",
  "rerunOnResume",
  "retryConfig",
  "timeout",
  "inputSchema",
  "outputSchema",
  "stateSchema",
  "isolationScope"
];
const NODE_DECLARED_KEYS = ["authConfig"];
function cloneWithOverrides(node, options) {
  const overrides = {};
  for (const key of OVERRIDABLE_KEYS) {
    if (options[key] !== void 0) {
      overrides[key] = options[key];
    }
  }
  for (const key of NODE_DECLARED_KEYS) {
    if (options[key] !== void 0 && key in node) {
      overrides[key] = options[key];
    }
  }
  if (Object.keys(overrides).length === 0) {
    return node;
  }
  if (typeof overrides["name"] === "string") {
    const name = overrides["name"].trim();
    if (!name) {
      throw new Error("Node name must be a non-empty string.");
    }
    overrides["name"] = name;
  }
  const clone = Object.create(
    Object.getPrototypeOf(node)
  );
  Object.assign(clone, node, overrides);
  if ("retryConfig" in overrides) {
    Object.assign(clone, {
      preparedRetryConfig: options.retryConfig ? prepareRetryConfig(options.retryConfig) : void 0
    });
  }
  return clone;
}
__name(cloneWithOverrides, "cloneWithOverrides");
export {
  buildNode,
  isNodeLike,
  isPlainObject
};
