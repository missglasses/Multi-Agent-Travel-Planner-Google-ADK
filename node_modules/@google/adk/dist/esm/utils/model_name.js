import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { getBooleanEnvVar } from "./env_aware_utils.js";
const MODEL_NAME_PATTERN = "^projects/[^/]+/locations/[^/]+/publishers/[^/]+/models/(.+)$";
function extractModelName(modelString) {
  const match = modelString.match(MODEL_NAME_PATTERN);
  if (match) {
    return match[1];
  }
  return modelString;
}
__name(extractModelName, "extractModelName");
function isGeminiModel(modelString) {
  const modelName = extractModelName(modelString);
  return modelName.startsWith("gemini-");
}
__name(isGeminiModel, "isGeminiModel");
function parseVersion(versionString) {
  if (!/^\d+(\.\d+)*$/.test(versionString)) {
    return { valid: false, major: 0, minor: 0, patch: 0 };
  }
  const parts = versionString.split(".").map((part) => parseInt(part, 10));
  return {
    valid: true,
    major: parts[0],
    minor: parts.length > 1 ? parts[1] : 0,
    patch: parts.length > 2 ? parts[2] : 0
  };
}
__name(parseVersion, "parseVersion");
function isGemini1Model(modelString) {
  const modelName = extractModelName(modelString);
  return modelName.startsWith("gemini-1");
}
__name(isGemini1Model, "isGemini1Model");
function isGemini2OrAbove(modelString) {
  if (!modelString) {
    return false;
  }
  const modelName = extractModelName(modelString);
  if (!modelName.startsWith("gemini-")) {
    return false;
  }
  const versionString = modelName.slice("gemini-".length).split("-", 1)[0];
  const parsedVersion = parseVersion(versionString);
  return parsedVersion.valid && parsedVersion.major >= 2;
}
__name(isGemini2OrAbove, "isGemini2OrAbove");
function isGemini3xFlashLive(modelString) {
  if (!modelString) {
    return false;
  }
  const modelName = extractModelName(modelString);
  return modelName.startsWith("gemini-3.") && modelName.includes("-flash-live");
}
__name(isGemini3xFlashLive, "isGemini3xFlashLive");
function isGeminiModelIdCheckDisabled() {
  return getBooleanEnvVar("ADK_DISABLE_GEMINI_MODEL_ID_CHECK");
}
__name(isGeminiModelIdCheckDisabled, "isGeminiModelIdCheckDisabled");
export {
  extractModelName,
  isGemini1Model,
  isGemini2OrAbove,
  isGemini3xFlashLive,
  isGeminiModel,
  isGeminiModelIdCheckDisabled
};
