import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { isEnterpriseModeEnabled } from "./env_aware_utils.js";
const EXPRESS_MODE_UNSUPPORTED_MESSAGE = "Vertex AI Express Mode (expressModeApiKey / GOOGLE_API_KEY) is not supported: the @google-cloud/vertexai Agent Engine client cannot send an API key. Provide projectId and location (with Application Default Credentials), or inject a preconfigured client.";
function getExpressModeApiKey(project, location, expressModeApiKey) {
  if ((project || location) && expressModeApiKey) {
    throw new Error(
      "Cannot specify project or location and expressModeApiKey. Either use project and location, or just the expressModeApiKey."
    );
  }
  if (isEnterpriseModeEnabled()) {
    return expressModeApiKey || process.env.GOOGLE_API_KEY;
  }
  return void 0;
}
__name(getExpressModeApiKey, "getExpressModeApiKey");
export {
  EXPRESS_MODE_UNSUPPORTED_MESSAGE,
  getExpressModeApiKey
};
