import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { Task } from "../utils/task.js";
const _ActiveStreamingTool = class _ActiveStreamingTool {
  constructor(params = {}) {
    if (params.task instanceof Promise) {
      this.task = new Task(() => params.task);
    } else {
      this.task = params.task;
    }
    this.stream = params.stream;
  }
};
__name(_ActiveStreamingTool, "ActiveStreamingTool");
let ActiveStreamingTool = _ActiveStreamingTool;
export {
  ActiveStreamingTool
};
