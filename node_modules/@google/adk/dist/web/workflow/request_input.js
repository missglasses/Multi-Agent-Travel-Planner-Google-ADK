import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
var _a;
import { randomUUID } from "../utils/env_aware_utils.js";
const REQUEST_INPUT_SIGNATURE_SYMBOL = Symbol.for(
  "google.adk.workflow.requestInput"
);
_a = REQUEST_INPUT_SIGNATURE_SYMBOL;
const _RequestInput = class _RequestInput {
  constructor(params = {}) {
    /** Brand identifying this object as a {@link RequestInput} (see {@link isRequestInput}). */
    this[_a] = true;
    var _a2;
    this.interruptId = (_a2 = params.interruptId) != null ? _a2 : randomUUID();
    this.payload = params.payload;
    this.message = params.message;
    this.responseSchema = params.responseSchema;
  }
};
__name(_RequestInput, "RequestInput");
let RequestInput = _RequestInput;
function isRequestInput(value) {
  return typeof value === "object" && value !== null && REQUEST_INPUT_SIGNATURE_SYMBOL in value && value[REQUEST_INPUT_SIGNATURE_SYMBOL] === true;
}
__name(isRequestInput, "isRequestInput");
export {
  RequestInput,
  isRequestInput
};
