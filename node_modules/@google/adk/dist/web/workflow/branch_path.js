import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
const _BranchPath = class _BranchPath {
  constructor(segments) {
    this.segments = [...segments];
  }
  toString() {
    return this.segments.join(".");
  }
  /** Returns a copy of the path segments. */
  getSegments() {
    return [...this.segments];
  }
  /** Whether this path is a strict descendant of `ancestor`. */
  isDescendantOf(ancestor) {
    if (this.segments.length <= ancestor.segments.length) {
      return false;
    }
    return ancestor.segments.every((seg, i) => this.segments[i] === seg);
  }
  /** Returns a new path with a `name@runId` (or `name`) segment appended. */
  append(name, runId) {
    const segment = runId !== void 0 ? "".concat(name, "@").concat(runId) : name;
    return new _BranchPath([...this.segments, segment]);
  }
};
__name(_BranchPath, "BranchPath");
let BranchPath = _BranchPath;
function branchPathFromString(path) {
  if (!path) {
    return new BranchPath([]);
  }
  return new BranchPath(path.split("."));
}
__name(branchPathFromString, "branchPathFromString");
function commonPrefixOfPaths(paths) {
  if (paths.length === 0) {
    return new BranchPath([]);
  }
  const allSegments = paths.map((p) => p.getSegments());
  const common = [];
  const minLen = Math.min(...allSegments.map((s) => s.length));
  for (let i = 0; i < minLen; i++) {
    const seg = allSegments[0][i];
    if (allSegments.every((s) => s[i] === seg)) {
      common.push(seg);
    } else {
      break;
    }
  }
  return new BranchPath(common);
}
__name(commonPrefixOfPaths, "commonPrefixOfPaths");
function createSubBranch(baseBranch, options) {
  return branchPathFromString(baseBranch).append(options.name, options.runId).toString();
}
__name(createSubBranch, "createSubBranch");
function commonPrefixOf(branches) {
  return commonPrefixOfPaths(
    branches.map((b) => branchPathFromString(b))
  ).toString();
}
__name(commonPrefixOf, "commonPrefixOf");
export {
  BranchPath,
  branchPathFromString,
  commonPrefixOf,
  commonPrefixOfPaths,
  createSubBranch
};
