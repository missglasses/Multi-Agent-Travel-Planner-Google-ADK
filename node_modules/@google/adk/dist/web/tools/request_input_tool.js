import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { z } from "zod";
import { REQUEST_INPUT_FUNCTION_CALL_NAME } from "../agents/functions.js";
import { LongRunningFunctionTool } from "./long_running_tool.js";
const requestInputTool = new LongRunningFunctionTool({
  name: REQUEST_INPUT_FUNCTION_CALL_NAME,
  description: "Presents a custom message to the user and awaits unstructured or structured input.",
  parameters: z.object({
    message: z.string().describe("The prompt for the user."),
    response_schema: z.record(z.string(), z.unknown()).optional().describe("Expected schema of the response.")
  }),
  execute: /* @__PURE__ */ __name(async (input, context) => {
    if (context) {
      context.actions.skipSummarization = true;
    }
    return null;
  }, "execute")
});
export {
  requestInputTool
};
