import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { Type } from "@google/genai";
import { appendInstructions } from "../models/llm_request.js";
import {
  BaseTool
} from "./base_tool.js";
const FINISH_TASK_TOOL_NAME = "finish_task";
const FINISH_TASK_SUCCESS_RESULT = "Task completed.";
const DEFAULT_TASK_OUTPUT_SCHEMA = {
  type: Type.OBJECT,
  properties: {
    result: {
      type: Type.STRING,
      description: "A brief summary of what the agent accomplished."
    }
  },
  required: ["result"]
};
const _FinishTaskTool = class _FinishTaskTool extends BaseTool {
  constructor(outputSchema) {
    const schema = outputSchema != null ? outputSchema : DEFAULT_TASK_OUTPUT_SCHEMA;
    let description = "Signal that this agent has completed its delegated task. Call this when you have finished your delegated task.";
    if (outputSchema) {
      description += " Pass the required output data in the parameters.";
    }
    super({ name: FINISH_TASK_TOOL_NAME, description });
    this.outputSchema = schema;
    this.wrapperKey = schema.type === Type.OBJECT ? void 0 : "result";
  }
  _getDeclaration() {
    const parameters = this.wrapperKey ? {
      type: Type.OBJECT,
      properties: { [this.wrapperKey]: this.outputSchema },
      required: [this.wrapperKey]
    } : this.outputSchema;
    return { name: this.name, description: this.description, parameters };
  }
  async processLlmRequest(request) {
    await super.processLlmRequest(request);
    appendInstructions(request.llmRequest, [
      "Do NOT call `finish_task` prematurely. Use your available tools to fully complete every aspect of the task first. If the task is unclear, ask the user for clarification before proceeding. Once the task is fully complete, call `finish_task` by itself with no accompanying text output."
    ]);
  }
  /**
   * Extracts the task output from a `finish_task` call's arguments, applying the
   * wrapper-key unwrapping when the schema is a non-object.
   */
  extractOutput(args) {
    if (this.wrapperKey) {
      return args[this.wrapperKey];
    }
    return args;
  }
  async runAsync({ args }) {
    const value = this.wrapperKey ? args[this.wrapperKey] : args;
    const missing = this.missingRequiredKeys(value);
    if (missing.length > 0) {
      return {
        error: "Invoking `".concat(this.name, "()` failed due to missing required ") + "parameters: ".concat(missing.join(", "), ". You could retry calling this ") + "tool, but it is IMPORTANT for you to provide all the mandatory parameters with correct types."
      };
    }
    return FINISH_TASK_SUCCESS_RESULT;
  }
  /** Returns any `required` keys the schema declares that are absent. */
  missingRequiredKeys(value) {
    var _a;
    const required = this.wrapperKey ? value === void 0 || value === null ? [this.wrapperKey] : [] : (_a = this.outputSchema.required) != null ? _a : [];
    if (this.wrapperKey) {
      return required;
    }
    if (typeof value !== "object" || value === null) {
      return required;
    }
    const obj = value;
    return required.filter((key) => obj[key] === void 0);
  }
};
__name(_FinishTaskTool, "FinishTaskTool");
let FinishTaskTool = _FinishTaskTool;
export {
  FINISH_TASK_SUCCESS_RESULT,
  FINISH_TASK_TOOL_NAME,
  FinishTaskTool
};
