import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { buildExampleSi } from "../examples/example_util.js";
import { appendInstructions } from "../models/llm_request.js";
import {
  BaseTool
} from "./base_tool.js";
const _ExampleTool = class _ExampleTool extends BaseTool {
  constructor(examples) {
    super({
      // Name and description are not used because this tool only changes
      // llmRequest.
      name: "example_tool",
      description: "example tool"
    });
    this.examples = examples;
  }
  async runAsync(_request) {
    throw new Error("ExampleTool should not be called by model");
  }
  async processLlmRequest({
    toolContext,
    llmRequest
  }) {
    var _a, _b;
    const parts = (_a = toolContext.userContent) == null ? void 0 : _a.parts;
    if (!parts || !((_b = parts[0]) == null ? void 0 : _b.text)) {
      return;
    }
    appendInstructions(llmRequest, [
      buildExampleSi(this.examples, parts[0].text, llmRequest.model)
    ]);
  }
};
__name(_ExampleTool, "ExampleTool");
let ExampleTool = _ExampleTool;
export {
  ExampleTool
};
