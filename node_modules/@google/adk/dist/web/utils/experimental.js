import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { logger } from "./logger.js";
const warnedItems = /* @__PURE__ */ new Set();
function experimental(target, propertyKey, descriptor) {
  var _a;
  if (propertyKey === void 0) {
    const className = target.name;
    const newConstructor = (_a = class extends target {
      constructor(...args) {
        if (!warnedItems.has(className)) {
          logger.warn(
            "Class ".concat(className, " is experimental and may change in the future.")
          );
          warnedItems.add(className);
        }
        super(...args);
      }
    }, __name(_a, "newConstructor"), _a);
    Object.defineProperty(newConstructor, "name", {
      value: className,
      configurable: true
    });
    return newConstructor;
  }
  if (propertyKey !== void 0 && descriptor !== void 0) {
    const className = typeof target === "function" ? target.name : target.constructor.name;
    const methodName = String(propertyKey);
    const identifier = "".concat(className, ".").concat(methodName);
    const originalMethod = descriptor.value;
    descriptor.value = function(...args) {
      if (!warnedItems.has(identifier)) {
        logger.warn(
          "Method ".concat(identifier, " is experimental and may change in the future.")
        );
        warnedItems.add(identifier);
      }
      return originalMethod.apply(this, args);
    };
    return descriptor;
  }
  throw new Error("Invalid decorator usage");
}
__name(experimental, "experimental");
export {
  experimental
};
