import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
const MODULE_NOT_FOUND_CODES = /* @__PURE__ */ new Set([
  "ERR_MODULE_NOT_FOUND",
  "MODULE_NOT_FOUND"
]);
function isMissingModule(err, packageName) {
  if (!(err instanceof Error)) {
    return false;
  }
  const code = err.code;
  return code !== void 0 && MODULE_NOT_FOUND_CODES.has(code) && err.message.includes(packageName);
}
__name(isMissingModule, "isMissingModule");
async function loadOptionalPeer(peer, load) {
  try {
    return await load();
  } catch (err) {
    if (!isMissingModule(err, peer.packageName)) {
      throw err;
    }
    throw new Error(
      "".concat(peer.feature, " requires the optional peer dependency ") + '"'.concat(peer.packageName, '", which is not installed. It is optional so ') + "that applications that do not use ".concat(peer.feature, " are not made to ") + "download it. Install it with:\n\n" + "  npm install ".concat(peer.packageName, "\n"),
      { cause: err }
    );
  }
}
__name(loadOptionalPeer, "loadOptionalPeer");
export {
  loadOptionalPeer
};
