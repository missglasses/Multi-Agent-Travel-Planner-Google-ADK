/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { Schema } from '@google/genai';
import { z as z3 } from 'zod/v3';
import { z as z4 } from 'zod/v4';
type ZodSchema<T = unknown> = z3.ZodType<T> | z4.ZodType<T>;
/** Returns true if the value is a Zod schema (v3 or v4). */
export declare function isZodSchema(obj: unknown): obj is ZodSchema;
/** Returns true if the value is a Zod v3 schema. */
export declare function isZodV3Schema(obj: unknown): obj is z3.ZodTypeAny;
/** Returns true if the value is a Zod v4 schema. */
export declare function isZodV4Schema(obj: unknown): obj is z4.ZodType;
/**
 * Returns true if the given object is a ZodObject (supports both Zod v3 and v4).
 */
export declare function isZodObject(obj: unknown): obj is z3.ZodObject<z3.ZodRawShape> | z4.ZodObject<z4.ZodRawShape>;
export declare function zodObjectToSchema(schema: z3.ZodObject<z3.ZodRawShape> | z4.ZodObject<z4.ZodRawShape>): Schema;
export {};
