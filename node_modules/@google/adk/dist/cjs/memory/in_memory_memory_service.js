/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var in_memory_memory_service_exports = {};
__export(in_memory_memory_service_exports, {
  InMemoryMemoryService: () => InMemoryMemoryService
});
module.exports = __toCommonJS(in_memory_memory_service_exports);
/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
const _InMemoryMemoryService = class _InMemoryMemoryService {
  constructor() {
    this.memories = [];
    /**
     * A map from user key to a map from session ID to events.
     *
     * The inner map is keyed by `session.id`, which arrives off the request path
     * and contains no `/`, so it can be exactly `__proto__`. On a plain object
     * literal that key would reach the inherited `__proto__` setter and
     * re-parent the map instead of creating an own property, silently dropping
     * the session from the `Object.values` scan in `searchMemory`. The outer key
     * always contains a `/` (see `getUserKey`) and cannot collide, but is
     * created the same way for consistency.
     */
    this.sessionEvents = /* @__PURE__ */ Object.create(null);
  }
  async addSessionToMemory(session) {
    const userKey = getUserKey(session.appName, session.userId);
    if (!this.sessionEvents[userKey]) {
      this.sessionEvents[userKey] = /* @__PURE__ */ Object.create(null);
    }
    this.sessionEvents[userKey][session.id] = session.events.filter(
      (event) => {
        var _a, _b, _c;
        return ((_c = (_b = (_a = event.content) == null ? void 0 : _a.parts) == null ? void 0 : _b.length) != null ? _c : 0) > 0;
      }
    );
  }
  async searchMemory(req) {
    var _a, _b;
    const userKey = getUserKey(req.appName, req.userId);
    if (!this.sessionEvents[userKey]) {
      return Promise.resolve({ memories: [] });
    }
    const wordsInQuery = req.query.toLowerCase().split(/\s+/);
    const response = { memories: [] };
    for (const sessionEvents of Object.values(this.sessionEvents[userKey])) {
      for (const event of sessionEvents) {
        if (!((_b = (_a = event.content) == null ? void 0 : _a.parts) == null ? void 0 : _b.length)) {
          continue;
        }
        const joinedText = event.content.parts.map((part) => part.text).filter((text) => !!text).join(" ");
        const wordsInEvent = extractWordsLower(joinedText);
        if (!wordsInEvent.size) {
          continue;
        }
        const matchQuery = wordsInQuery.some(
          (queryWord) => wordsInEvent.has(queryWord)
        );
        if (matchQuery) {
          response.memories.push({
            content: event.content,
            author: event.author,
            timestamp: formatTimestamp(event.timestamp)
          });
        }
      }
    }
    return response;
  }
};
__name(_InMemoryMemoryService, "InMemoryMemoryService");
let InMemoryMemoryService = _InMemoryMemoryService;
function getUserKey(appName, userId) {
  return `${appName}/${userId}`;
}
__name(getUserKey, "getUserKey");
function extractWordsLower(text) {
  return new Set(
    [...text.matchAll(/[A-Za-z]+/g)].map((match) => match[0].toLowerCase())
  );
}
__name(extractWordsLower, "extractWordsLower");
function formatTimestamp(timestamp) {
  return new Date(timestamp).toISOString();
}
__name(formatTimestamp, "formatTimestamp");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  InMemoryMemoryService
});
