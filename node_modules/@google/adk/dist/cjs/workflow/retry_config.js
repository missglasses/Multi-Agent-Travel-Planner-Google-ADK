/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var retry_config_exports = {};
__export(retry_config_exports, {
  normalizeRetryExceptions: () => normalizeRetryExceptions,
  prepareRetryConfig: () => prepareRetryConfig
});
module.exports = __toCommonJS(retry_config_exports);
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
function normalizeRetryExceptions(exceptions) {
  if (exceptions === void 0 || exceptions === null) {
    return void 0;
  }
  return exceptions.map((item) => {
    if (typeof item === "string") {
      return item;
    }
    if (typeof item === "function" && item.name) {
      return item.name;
    }
    throw new Error(
      `exceptions must contain error class names (string) or error classes, got: ${String(
        item
      )}`
    );
  });
}
__name(normalizeRetryExceptions, "normalizeRetryExceptions");
function prepareRetryConfig(config) {
  return {
    maxAttempts: config.maxAttempts,
    initialDelay: config.initialDelay,
    maxDelay: config.maxDelay,
    backoffFactor: config.backoffFactor,
    jitter: config.jitter,
    exceptions: normalizeRetryExceptions(config.exceptions)
  };
}
__name(prepareRetryConfig, "prepareRetryConfig");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  normalizeRetryExceptions,
  prepareRetryConfig
});
