/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var optional_peer_exports = {};
__export(optional_peer_exports, {
  loadOptionalPeer: () => loadOptionalPeer
});
module.exports = __toCommonJS(optional_peer_exports);
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
const MODULE_NOT_FOUND_CODES = /* @__PURE__ */ new Set([
  "ERR_MODULE_NOT_FOUND",
  "MODULE_NOT_FOUND"
]);
function isMissingModule(err, packageName) {
  if (!(err instanceof Error)) {
    return false;
  }
  const code = err.code;
  return code !== void 0 && MODULE_NOT_FOUND_CODES.has(code) && err.message.includes(packageName);
}
__name(isMissingModule, "isMissingModule");
async function loadOptionalPeer(peer, load) {
  try {
    return await load();
  } catch (err) {
    if (!isMissingModule(err, peer.packageName)) {
      throw err;
    }
    throw new Error(
      `${peer.feature} requires the optional peer dependency "${peer.packageName}", which is not installed. It is optional so that applications that do not use ${peer.feature} are not made to download it. Install it with:

  npm install ${peer.packageName}
`,
      { cause: err }
    );
  }
}
__name(loadOptionalPeer, "loadOptionalPeer");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  loadOptionalPeer
});
