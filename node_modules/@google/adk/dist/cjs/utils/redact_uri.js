/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var redact_uri_exports = {};
__export(redact_uri_exports, {
  redactUriPassword: () => redactUriPassword
});
module.exports = __toCommonJS(redact_uri_exports);
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
const SECRET_QUERY_PARAMS = /* @__PURE__ */ new Set([
  "password",
  "passwd",
  "pwd",
  "sslpassword"
]);
function redactUriPassword(uri) {
  try {
    const url = new URL(uri);
    let redacted = false;
    if (url.password) {
      url.password = "***";
      redacted = true;
    }
    for (const name of [...url.searchParams.keys()]) {
      if (SECRET_QUERY_PARAMS.has(name.toLowerCase())) {
        url.searchParams.set(name, "***");
        redacted = true;
      }
    }
    return redacted ? url.toString() : uri;
  } catch {
    const schemeEnd = uri.indexOf("://");
    return schemeEnd === -1 ? "<unparseable URI, redacted>" : `${uri.slice(0, schemeEnd)}://<unparseable URI, redacted>`;
  }
}
__name(redactUriPassword, "redactUriPassword");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  redactUriPassword
});
