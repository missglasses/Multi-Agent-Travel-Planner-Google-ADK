/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var compaction_utils_exports = {};
__export(compaction_utils_exports, {
  calculateRetainStartIndex: () => calculateRetainStartIndex,
  getActiveEvents: () => getActiveEvents
});
module.exports = __toCommonJS(compaction_utils_exports);
var import_compacted_event = require("../events/compacted_event.js");
var import_event = require("../events/event.js");
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
function getActiveEvents(events) {
  const latest = events.filter(import_compacted_event.isCompactedEvent).pop();
  return latest ? [
    latest,
    ...events.filter(
      (e) => !(0, import_compacted_event.isCompactedEvent)(e) && e.timestamp > latest.endTime
    )
  ] : events;
}
__name(getActiveEvents, "getActiveEvents");
function calculateRetainStartIndex(rawEvents, eventRetentionSize) {
  let retainStartIndex = Math.max(0, rawEvents.length - eventRetentionSize);
  while (retainStartIndex > 0) {
    const eventToRetain = rawEvents[retainStartIndex];
    const previousEvent = rawEvents[retainStartIndex - 1];
    if ((0, import_event.getFunctionResponses)(eventToRetain).length > 0 && (0, import_event.getFunctionCalls)(previousEvent).length > 0) {
      retainStartIndex--;
    } else {
      break;
    }
  }
  return retainStartIndex;
}
__name(calculateRetainStartIndex, "calculateRetainStartIndex");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  calculateRetainStartIndex,
  getActiveEvents
});
