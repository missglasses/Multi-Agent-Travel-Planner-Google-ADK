/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var invocation_context_exports = {};
__export(invocation_context_exports, {
  InvocationContext: () => InvocationContext,
  newInvocationContextId: () => newInvocationContextId,
  requireAgent: () => requireAgent
});
module.exports = __toCommonJS(invocation_context_exports);
var import_env_aware_utils = require("../utils/env_aware_utils.js");
/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
const _InvocationCostManager = class _InvocationCostManager {
  constructor() {
    this.numberOfLlmCalls = 0;
  }
  /**
   * Increments the number of llm calls and enforces the limit.
   *
   * @param runConfig the run config of the invocation.
   * @throws If number of llm calls made exceed the set threshold.
   */
  incrementAndEnforceLlmCallsLimit(runConfig) {
    this.numberOfLlmCalls++;
    if (runConfig && runConfig.maxLlmCalls > 0 && this.numberOfLlmCalls > runConfig.maxLlmCalls) {
      throw new Error(
        `Max number of llm calls limit of ${runConfig.maxLlmCalls} exceeded`
      );
    }
  }
};
__name(_InvocationCostManager, "InvocationCostManager");
let InvocationCostManager = _InvocationCostManager;
const _InvocationContext = class _InvocationContext {
  /**
   * @param params The parameters for creating an invocation context.
   */
  constructor(params) {
    var _a, _b;
    this.artifactService = params.artifactService;
    this.sessionService = params.sessionService;
    this.memoryService = params.memoryService;
    this.invocationId = params.invocationId;
    this.branch = params.branch;
    this.agent = params.agent;
    this.userContent = params.userContent;
    this.session = params.session;
    this.endInvocation = params.endInvocation || false;
    this.transcriptionCache = params.transcriptionCache;
    this.runConfig = params.runConfig;
    this.activeStreamingTools = params.activeStreamingTools;
    this.pluginManager = params.pluginManager;
    this.abortSignal = params.abortSignal;
    this.workflowInstructionScope = params.workflowInstructionScope;
    this.isolationScope = params.isolationScope;
    this.nodeToolDepth = (_a = params.nodeToolDepth) != null ? _a : 0;
    this.invocationCostManager = (_b = params.invocationCostManager) != null ? _b : new InvocationCostManager();
    this.liveRequestQueue = params.liveRequestQueue;
    this.liveSessionResumptionHandle = params.liveSessionResumptionHandle;
  }
  /**
   * The app name of the current session.
   */
  get appName() {
    return this.session.appName;
  }
  /**
   * The user ID of the current session.
   */
  get userId() {
    return this.session.userId;
  }
  /**
   * Tracks number of llm calls made.
   *
   * @throws If number of llm calls made exceed the set threshold.
   */
  incrementLlmCallCount() {
    this.invocationCostManager.incrementAndEnforceLlmCallsLimit(this.runConfig);
  }
  /**
   * Returns a copy of this context with `overrides` applied. The spread carries
   * every own field over (including the shared cost manager), so the copy keeps
   * a single LLM-call counter for the invocation.
   *
   * Note: this copies own enumerable fields by value — scalar mutable fields
   * (e.g. `endInvocation`) are decoupled from the original, while object-valued
   * fields (`session`, …) stay shared by reference.
   */
  clone(overrides = {}) {
    return new _InvocationContext({ ...this, ...overrides });
  }
};
__name(_InvocationContext, "InvocationContext");
let InvocationContext = _InvocationContext;
function newInvocationContextId() {
  return `e-${(0, import_env_aware_utils.randomUUID)()}`;
}
__name(newInvocationContextId, "newInvocationContextId");
function requireAgent(ctx) {
  if (!ctx.agent) {
    throw new Error(
      "InvocationContext.agent is not set: this invocation is running a node directly, so there is no agent at this level."
    );
  }
  return ctx.agent;
}
__name(requireAgent, "requireAgent");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  InvocationContext,
  newInvocationContextId,
  requireAgent
});
