/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var credential_response_binding_exports = {};
__export(credential_response_binding_exports, {
  bindCredentialResponse: () => bindCredentialResponse
});
module.exports = __toCommonJS(credential_response_binding_exports);
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
function readField(source, name) {
  if (typeof source !== "object" || source === null) {
    return void 0;
  }
  const record = source;
  if (name in record) {
    return record[name];
  }
  return record[name.replace(/[A-Z]/g, (c) => `_${c.toLowerCase()}`)];
}
__name(readField, "readField");
function readString(source, name) {
  const value = readField(source, name);
  return typeof value === "string" ? value : void 0;
}
__name(readString, "readString");
function bindCredential(request, supplied) {
  var _a;
  const requested = request.exchangedAuthCredential;
  if (!((_a = requested == null ? void 0 : requested.oauth2) == null ? void 0 : _a.authUri)) {
    return supplied;
  }
  const suppliedOAuth2 = readField(supplied, "oauth2");
  const answer = {};
  const authResponseUri = readString(suppliedOAuth2, "authResponseUri");
  if (authResponseUri !== void 0) {
    answer.authResponseUri = authResponseUri;
  }
  const authCode = readString(suppliedOAuth2, "authCode");
  if (authCode !== void 0) {
    answer.authCode = authCode;
  }
  if (authResponseUri === void 0 && authCode === void 0) {
    return void 0;
  }
  return {
    ...requested,
    oauth2: { ...requested.oauth2, ...answer }
  };
}
__name(bindCredential, "bindCredential");
function bindCredentialResponse(request, response) {
  const { authScheme, credentialKey } = request;
  if (!authScheme || !credentialKey) {
    return void 0;
  }
  const supplied = readField(response, "exchangedAuthCredential");
  if (typeof supplied !== "object" || supplied === null) {
    return void 0;
  }
  const exchangedAuthCredential = bindCredential(request, supplied);
  if (!exchangedAuthCredential) {
    return void 0;
  }
  return {
    authScheme,
    credentialKey,
    rawAuthCredential: request.rawAuthCredential,
    exchangedAuthCredential
  };
}
__name(bindCredentialResponse, "bindCredentialResponse");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  bindCredentialResponse
});
